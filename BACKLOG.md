# Backlog

## Done
Entries before 2026-08-09 are in `BACKLOG_ARCHIVE.md`.

- [x] 2026-08-09 — Phase 0 of `houdini/docs/HOUDINI_MCP_REWRITE_PLAN.md`: repo restructured into `houdini/` and `nuke/` subtrees (bridge/plugin/docs/CLAUDE.md each), `little_helpers/` extracted with history (`git filter-repo`) into its own repo (`github.com/sandalya/little_helpers`), checked out as a sibling `../little_helpers` — `deploy_plugin.sh nuke` now reads it from there. `nuke_overlay.py` deleted (superseded PoC). Root `CLAUDE.md` trimmed to shared doctrine; domain rules moved to `houdini/CLAUDE.md`/`nuke/CLAUDE.md`. Claude Desktop config repointed at the new bridge path. Live-verified end to end once VPN came back up: `deploy_plugin.sh all` succeeded (backup+scp both targets, hython `py_compile` clean on the Houdini side), and the full MCP-protocol smoke test against the redeployed Nuke plugin passed all 5 calls including `nuke_get_nodes_in_view`, which lazy-imports the now-external `little_helpers` — confirming the extraction works in production, not just locally. Caught and fixed a real regression the move introduced: both bridge scripts locate their `.venv` via a path relative to their own file, which broke when they moved into `houdini/bridge/`/`nuke/bridge/` (silently fell back to the caller's `sys.path`, so it never crashed, but printed a false warning); the Houdini bridge additionally used that same broken path for `get_project_context`'s README.md read and the CC-inbox `notes/` dir, which would have served stale/missing content — fixed all three to resolve off the repo root instead, verified live via a direct `get_project_context` call returning the current 18k-char README. Houdini plugin itself not live-tested past the syntax check — the in-scene MCP server isn't running yet (no auto-start, by design; needs a manual start from Houdini, which is Sashok's step).
- [x] 2026-08-09 — Phase 1 of `houdini/docs/HOUDINI_MCP_REWRITE_PLAN.md`, code complete and deployed (not yet live-verified): new `houdini/plugin/hmcp/` package (port 9878, alongside the untouched old plugin on 9876) — `guards.py` (full safety layer: sandbox-boundary checks, never-saved-scene bootstrap detection via confirmed-present `hou.hipFile.isNewFile()`, output/input/code parm whitelists, `SessionRegistry` keyed on `node.sessionId()` — none of it called yet since Phase 1 is read-only), `intro.py` (`get_scene_info`, `get_geometry_info`, `get_node_errors`, `get_node_type_parms`, `list_node_types`, `get_node_help`, `get_network`, `get_node_info`, `describe_commands`), `commands.py` (dispatcher built from `houdini/commands_spec.py`, raises at import if the two disagree), `server.py`/`__init__.py` (socket+QTimer transport ported from the old plugin, no auto-start). New `houdini/commands_spec.py` is the single source of truth for command names/params, imported by both the plugin dispatcher and the new bridge (`houdini/bridge/hmcp_bridge.py`, registered in Claude Code as `houdini2` so the Claude Desktop `houdini` entry is untouched) — the bridge asserts its own tool set against it at startup, and `scripts/check_contract.py` (new) diffs the live plugin's `describe_commands` response against it too, so `connect_nodes`-style bridge/plugin drift fails loudly at three separate points instead of silently. `deploy_plugin.sh` gained an `hmcp` target (deploys the package + `commands_spec.py`, runs `hython -m py_compile` on every file, then calls `check_contract.py`). Deployed clean: all 6 files `py_compile` pass on pc137. `check_contract.py` correctly refused to connect (server not started yet — needs Sashok to open a sandbox scene and call `hmcp.start_server()`, same manual-start rule as the old plugin). Found `C:/houdini_mcp_sandbox/_snapshots/` doesn't exist yet on pc137 — not needed until Phase 2's `viewport_snapshot`, flagged for Sashok to create per the plan's "owner creates it once, plugin never creates directories" rule. Still open: the plan's claim that `.claude/settings.local.json` already had `mcp__houdini__*` entries was stale (it didn't) — added `mcp__houdini2__*` fresh instead. Live verification (the 6-step Phase 1 checklist) pending Sashok starting the new server.
- [x] 2026-08-09 — Phase 1 live-verified end to end, all checklist items pass. New `scripts/mcp_client_test_hmcp.py` (stdio MCP protocol, same pattern as the Nuke one) exercises every tool through the real path: Claude Code → `hmcp_bridge.py` → socket → `hmcp` plugin → Houdini → back. Two real bugs found and fixed along the way: `get_geometry_info` on a non-SOP node (`'OpNode' object has no attribute 'geometry'`) and `get_node_type_parms` on a `ButtonParmTemplate` (`no attribute 'defaultValue'`) — both the same root cause, `_safe(obj.method)` evaluates `obj.method` as the call argument *before* `_safe`'s own try/except runs, so a genuinely-missing method (not just one that raises) slipped through uncaught. Fixed structurally with a new `_safe_attr(obj, name, *args, default=None)` helper (does the `getattr` check itself) and swept every `_safe(x.y)` call site in `intro.py` to use it. Also hit and fixed a transport bug: `self.socket.listen(1)` left zero slack for more than one connection attempt in flight at a time — several diagnostic connections landing close together during verification got refused outright; bumped to `listen(5)`. Confirmed via direct `Get-NetTCPConnection` on pc137 that Windows Firewall was a red herring (all profiles disabled) and the shared `houdini_mcp_audit.log`'s `BLOCKED_IP` lines during this session were old-plugin traffic, not hmcp (own IP `10.10.11.31` was correctly allowlisted throughout). Also confirmed live: `check_contract.py` matches (9/9 commands), all 5 files `py_compile` clean on every redeploy. Phase 1 is done — ready to start Phase 2 (write layer, `hmcp/build.py`) next.
- [x] 2026-08-09 — Closed the one Phase 1 checklist item that needed real geometry: Sashok hand-built a sphere (`/obj/sphere1/sphere1`) in the sandbox scene, `get_geometry_info` read it back correctly — 266 points, 288 prims, 1104 vertices, bbox exactly ±0.5, single `P` point attribute. Confirms the intrinsic-value-based counts and attribute serialization are correct on real data, not just the error path.
- [x] 2026-08-09 — Phase 2 of `houdini/docs/HOUDINI_MCP_REWRITE_PLAN.md`, code complete and deployed (not yet live-verified): new `houdini/plugin/hmcp/build.py` — 15 write handlers (`create_node`, `connect_nodes`, `set_parm`, `set_expression`, `delete_node`, cosmetic `rename_node`/`set_position`/`set_color`/`set_comment`/`layout_children`, flags `set_display_flag`/`set_render_flag`/`set_bypass`, bootstrap `save_scene_as`, `viewport_snapshot`), every one calling `guards.require_sandbox_scene()` first (except `save_scene_as`, gated by `require_bootstrap_scene` instead) and wrapping its mutation in `hou.undos.group(...)`. `create_node` deliberately takes no `parameters` dict — the old plugin's whitelist-bypass (`server.py:290-294` sets arbitrary parms at creation, skipping `SAFE_PARMS`) has no path back in because `set_parm` is now the only way to set a value and it always runs `guards.check_settable_parm`. Found and fixed a real Phase-1 bug before its first caller: `guards.ALLOWED_NODE_CATEGORIES` had `"Obj"`, but the real Houdini category name is `"Object"` (confirmed against proven code, `plugin/server.py:766`) — would have silently refused every `/obj` node creation. `commands_spec.py` gained the 15 write entries (`kind: "write"`), `commands.py`'s `_HANDLERS` wired to `build.py`, `hmcp_bridge.py` gained matching `@mcp.tool()` wrappers plus the 15 names in its drift-check assertion set. Deployed via `deploy_plugin.sh hmcp`: all 6 files `py_compile` clean on pc137; `check_contract.py` shows the expected mismatch (live plugin still running Phase-1-only code in memory — needs a reload in Houdini, not yet done). Bridge smoke-tested locally (`mcp_client_test_hmcp.py`, read-only): all 24 tools now exposed, no assertion failure, reads still work against the live (not-yet-reloaded) plugin on `dev_phase1_checks.hip`. Deliberately did **not** add the 15 write tools to `.claude/settings.local.json`'s auto-allow list — root `CLAUDE.md`'s safety rule calls out node deletion explicitly as needing a per-instance ask even when technically permitted; left as per-call prompts. Live verification (the 9-item Phase 2 checklist, esp. the three negative scene-state tests) needs Sashok to reload the plugin in Houdini and is deliberately not something to script unattended against his live sandbox scene.
- [x] 2026-08-10 — Phase 2 live-verified end to end, all 9 checklist items pass. Positive flow on `dev_phase1_checks.hip`: `create_node` under `/obj` and inside a `geo` (categories correctly resolved as `Object`/`Sop`, confirming the ALLOWED_NODE_CATEGORIES fix), `connect_nodes` (the capability that never existed before), `set_parm` (numeric + the `file` input-path allowlist), `set_expression` (HScript, confirmed via `get_node_info`'s `raw_value`), all cosmetic/flag commands, and `viewport_snapshot`. Negative flow, all confirmed refused with clean messages and logged to the audit log: non-sandbox scene (accidentally-real test — see incident below), an already-saved non-sandbox scene refusing `save_scene_as` too, `Driver`-category and `python`-type node creation, and `delete_node` on a node the current session didn't create. Positive `delete_node` on a session-created node succeeds. One Ctrl+Z fully removed a single-node create block, nothing else touched. Found and fixed a second real bug live: `hou.updateMode()` (copied verbatim from the old plugin's `HoudiniMCPRender.py:24`, believed "proven") is deprecated and outright uncallable on this Houdini build (21.0.729, newer than the 21.0.596 the rewrite plan was written against) — `hou.updateMode` resolves to the enum class only, calling it raises `AttributeError: No constructor defined`. Fixed by switching to `hou.updateModeSetting()`. The old plugin has the identical bug at the same line, untouched per the plan's migration rule (old plugin is out of scope until Phase 3) — flagged in TODO. Real incident along the way: right after hitting the (pre-fix) `updateMode` error, a `KeyboardInterrupt` in Houdini's Python Shell crashed the whole process (crash-recovery saved to a temp file, original sandbox `.hip` untouched); a red herring during recovery — repeated raw single-shot socket probes from this side made the server look permanently hung (`ConnectionRefusedError` once its `listen(5)` backlog filled with unreaped `CloseWait` connections from those same probes) even though Houdini itself was responsive — resolved by stopping probing and doing a clean `hmcp.stop_server()`/`start_server()` cycle. `mcp__houdini2__delete_node` calls are hard-blocked by the Claude Code auto-mode classifier regardless of in-chat confirmation (not a settings.local.json permission gap) — worked around by having Sashok call `hmcp.commands.REGISTRY["delete_node"]["handler"](...)` directly in Houdini's Python Shell for both the negative and positive delete tests. Not tested: the unsaved-scene `save_scene_as` bootstrap path (needs a genuinely untitled scene, deferred as low-risk/low-priority). Test scaffold left in the scene (`/obj/mcp_phase2_test`) pending Sashok's own cleanup pass. Ready for Phase 3 (unattended procedural-rock build) next.
- [x] 2026-08-10 — Phase 3 acceptance target met: `/obj/rock` built unattended on `dev_phase1_checks.hip` — `base_sphere` (polymesh, 1122pt) → `displace` (mountain::2.0) → `shape_vex` (attribwrangle, two-frequency directional-noise VEX) → `relax` (smooth::2.0, low strength). Read geometry/errors after each step, cooked clean throughout, finished with `viewport_snapshot`. First pass (default `smooth::2.0` strength=10) over-smoothed into a "potato," not a rock — Sashok's call; second pass (raised `displace` height/rough/oct, sharper two-octave VEX bump, `relax` strength dropped 10→1.2) confirmed visually rock-like via the snapshot. One Ctrl+Z removing the whole build not re-verified this round (already covered in the Phase 2 entry above, same per-handler undo-group mechanism, no new code path). Real process feedback from Sashok mid-session, more important than the geometry result: building node-by-node with a check-in after nearly every single call was far too slow (10-20 min for a small setup) — no upfront plan, improvising live. Fix applied immediately: planned the full remaining chain before touching tools, then executed in 2 large parallel tool-call batches instead of ~15 serial ones, cutting the second (fix-up) pass to a fraction of the first build's time. Saved as a standing feedback memory for future hmcp sessions.
- [x] 2026-08-10 — Reorganized `little_helpers`' version manager (Change Layer Version, Shift+E) out of the package root into its own `veriter/` subpackage (`versions.py` + `version_ui.py`, `git mv` with history preserved in the sibling `sandalya/little_helpers` repo), mirroring the existing `split_layers/` pattern — Sashok's ask, so the package reads as a suite (shared root infra + one subfolder per tool) instead of looking like the version manager *is* little_helpers, and so the next new tool has an obvious place to land. `little_helpers/__init__.py`'s `show_version_hud()` is now a lazy wrapper into `veriter.version_ui`, matching the `split_layers` lazy-import style; `_RELOAD_ORDER` updated. `docs/NUKE_PIPELINE_TD_INTEGRATION.md` file-placement tree rewritten for the new layout. `vfx-mcp/scripts/deploy_plugin.sh` gained a third `deploy_dir()` leg for `veriter/` (without it the version manager would have silently stopped deploying to pc137, since `deploy_dir()` isn't recursive). Verified statically only (`py_compile` on the moved/new files, `bash -n` on the deploy script) — live Shift+E smoke test on pc137 still pending.
- [x] 2026-08-10 — `veriter/` live-verified on pc137: deployed via `deploy_plugin.sh nuke` (backed up old `little_helpers` dir first), Sashok reloaded `little_helpers` in an already-open Nuke and confirmed Shift+E (Change Layer Version) works — no manual reload needed going forward since `register_menu()`'s command strings already call `little_helpers.reload_all()` on every keypress (`__init__.py:74-79`). Along the way found the MCP menu (`MCP/Server/MCP Server`, `ctrl+shift+t`) was missing — unrelated to the veriter move: pc137's `~/.nuke/menu.py` had `import nuke_mcp_plugin` / `nuke_mcp_plugin.register_menu()` commented out, so that `register_menu()` never ran (unlike `little_helpers.register_menu()`, right below it, which was active). Uncommented both lines directly on pc137 via ssh (backed up `menu.py` first), Sashok restarted Nuke (menu.py only runs at startup, no reload hook for it), confirmed both the veriter hotkeys and the MCP menu now work. Ready to hand off to TD.
- [x] 2026-08-10 — `veriter` bug found live: `Version -` on a selected Read (`Read5`) silently no-op'd, printing `skipped -- not live`. Root cause was working as designed, not a bug: `bump_selected_reads` skips any Read with nothing downstream, meant to protect the nodes-in-view fallback from bumping stray history Reads sitting next to their live sibling — but it applied the same gate even to an explicit selection, where Sashok picking a disconnected Read by hand is itself "act on this." Fixed in `veriter/versions.py`: the not-live skip now only fires on the nodes-in-view fallback (nothing selected); an explicit selection always bumps regardless of connectivity. History-row sync stays gated on liveness either way, so a selected history Read still won't spawn its own history chain (the exact cascade already fixed once before). Deployed via `deploy_plugin.sh nuke`, live-verified by Sashok via Shift+E's automatic `reload_all()` — no Nuke restart needed.
- [x] 2026-08-11 — hmcp dual-mode connection (pc137/VPN vs. local Houdini, no VPN): `HMCP_HOST` env var in `hmcp_bridge.py`/`check_contract.py` (default unchanged, `10.10.10.31`), new `deploy_plugin.sh hmcp-local` target (plain local copy + local hython py_compile, no ssh), PySide6/PySide2 compat shim in `hmcp/server.py` (local Houdini is 20.5.278/PySide2, pc137 is 21.0/PySide6 — confirmed both resolve live via hython import). Also widened `guards.is_sandbox_scene()`: now true under `SANDBOX_ROOT` *or* if the scene's own global variable `hmcp = 1` is set (checked via `hscript("set")`, not `hou.getenv`, so an ambient OS env var of the same name can't satisfy it) — lets a project-tree `.hip` opt in without being moved into the sandbox folder. Verified via hython against the locally-deployed package: PySide2 import resolves, opt-in variable flips `is_sandbox_scene()` true/false as expected. `~/.claude.json`'s `houdini2.env` set to `{"HMCP_HOST": "127.0.0.1"}` pending Claude Code restart. Not yet done: live round-trip against an actually-running local Houdini (`hmcp.start_server()` + `check_contract_local.sh` + a real MCP call from this session after restart).
- [x] 2026-08-12 — hmcp local mode fixed and live-verified: poll loop moved off a bare `QTimer` (never fires — Houdini's Python Shell pane isn't the main thread) onto `hou.ui.addEventLoopCallback`, audit log moved to the Houdini prefs dir, added `hmcp.status()` tick counter. `check_contract_local.sh` passes (24 commands), MCP `get_scene_info` round-trips. pc137 redeployed the same day (it was unaffected — Houdini 21's shell does run in the main thread, contract check passed there on the old code too).
- [x] 2026-08-12 — `houdini/shelf/hmcp.shelf` (start/restart + stop buttons) added to the repo and wired into both `deploy_plugin.sh` hmcp targets; deployed to this machine and pc137. Shelf tools run on the main thread, and the start button purges `hmcp*` from `sys.modules` so a deploy lands without restarting Houdini. Shelf tab still needs adding to the shelf set by hand once per machine.
- [x] 2026-08-12 — Ice-cave look-dev, geometry pattern pass (`/obj/ice_pattern_test`, sandbox scene): flat grid → density-driven scatter → voronoifracture → per-piece inset (polyextrude) → connectivity → per-piece random height gives a sharp-edged crack/facet pattern with mixed large+small cell sizes (bimodal noise-driven scatter density), matching the reference photo's fracture look. Pure geometry, no shading yet — confirmed via viewport_snapshot. Remaining from the same critique: SSS-like shading, fine frost noise, blue-to-white color gradient.
- [x] 2026-08-12 — Stage 0 of `houdini/docs/HMCP_FEEDBACK_LOOP_PLAN.md` complete: camera API (0b) and ROP parm names (D7) confirmed identical on local 20.5.278 and pc137 21.0.729; V1–V4 and the camera-lock guard precondition empirically resolved; Karma PNG render confirmed working foreground+background, Mantra found broken (SOHO export and `mantra.exe` both fine, render itself silently produces nothing) and recommended excluded from Stage 3; found and decided a real precondition not in either source doc — `executebackground` refuses under unsaved changes and, even once saved, blocks the main thread for 6.5–16.6s (not near-instant) — fix is `render_snapshot` auto-saving the hip immediately before pressing it.
- [x] 2026-08-12 — Stage 1 of `houdini/docs/HMCP_FEEDBACK_LOOP_PLAN.md` complete, live-verified (local 20.5.278): full tracebacks into `hmcp_audit.log` on unhandled exceptions, optional `hint` field on error responses, `connect_nodes` category pre-check (named refusal on cross-category wiring), shared `_require_scene_viewport()` helper, and its missing-SceneViewer/Manual-update-mode preflight checks changed from `RuntimeError` to `ValueError` with a `"Precondition: "` prefix so they get a clean `REFUSED` audit line instead of a raw traceback. `check_contract.py` still agrees on 24 commands (no new commands this stage). Along the way, found and diagnosed a real infra bug unrelated to the code changes: two orphaned listening sockets ended up bound to port 9878 in the same Houdini process after repeated `sys.modules`-purge reloads, causing connections to intermittently time out then get actively refused once the dead listener's backlog filled — no fix in code (the dead listener is unreachable without a debugger once its owning `HmcpServer` object falls out of scope); resolved by a full Houdini restart. pc137 still needs its own deploy + live pass.
- [x] 2026-08-12 — Stage 2 of `houdini/docs/HMCP_FEEDBACK_LOOP_PLAN.md` complete, live-verified (local 20.5.278): `get_viewport_info`, `viewport_frame_node`, `viewport_frame_all`, `viewport_set_view`, `viewport_dolly` all shipped (24 → 29 commands) and confirmed live — the acceptance test (frame an unframed object, then snapshot, hands off keyboard) passed, `viewport_frame_node` on an Object node vs. a deep SOP inside it returned bit-identical pivot/translation (non-identity object transform, catches the SOP-local-vs-world bug class), and every negative case in the plan's checklist confirmed refusing cleanly (no SceneViewer pane, camera-locked viewport incl. a stale-lock edge case found live where the camera node was deleted but `isCameraLockedToView()` stayed true, bad view names, no-getattr-passthrough, missing node, empty geo, wrong node category). `viewport_orbit` dropped instead of shipped: live-tested 4 independent rotation-matrix conventions for `cam.setRotation()` (delta composition, hand-built look-at + its transpose, `hou.hmath.buildRotate` applied directly from a verified-good identity baseline) and none kept the pivot centred, despite each being independently verified self-consistent and despite confirming `setRotation()` does affect the actual render — root cause not found this session, `viewport_dolly` (translation-only, no rotation math) shipped instead and works correctly. Also found and fixed, pre-testing: `cam.translation()`/`cam.pivot()` return plain tuples not `hou.Vector3`, breaking the vector arithmetic the plan's own Stage-0-derived formula assumed.
- [x] 2026-08-12 — `viewport_orbit` root-caused and fixed same day (re-added, 24 → 30 commands total): an Opus subagent found, via SideFX staff on the forum, that `hou.GeometryViewportCamera.rotation()`/`.translation()`'s HOM docs are simply wrong — `rotation()` is world-to-camera (not camera-to-world), and `translation()` is `pivot + (eye - pivot)` expressed in camera space (not a world position), which is why every prior attempt double-applied the orbit. Fix is rotation-only, pre-multiplied by the delta's inverse (`cam.setRotation(delta3.inverted() * cam.rotation())`), translation left untouched entirely since centring is structural. Live-confirmed: 90° single-axis orbit and a cumulative `dx=45,dy=20` on top of it both stayed exactly centred, translation unchanged both times as the model predicts.
- [x] 2026-08-12 — Stage 3 of `houdini/docs/HMCP_FEEDBACK_LOOP_PLAN.md` (`render_snapshot`/`render_status`, 30 → 32 commands) live-verified end to end (local 20.5.278): background Karma renders confirmed non-overwriting across calls, `render_status()` clean with nothing pending, second-render-while-pending refusal, `set_parm` still refuses the `picture` parm, `camera="viewport"` framing matches `viewport_snapshot`. Found and fixed two real bugs live. (1) The planned `f1==f2==hou.frame()` belt-and-braces frame-pin is a structural no-op on the Karma HDA — it keeps `f1`/`f2` synced to `$FSTART`/`$FEND` and silently reverts any direct `.set()` on them regardless of `trange`'s state (confirmed by direct `set_parm` attempts, both with `trange` off and on); removed the dead code, confirmed the actual guard (forcing `trange="off"` unconditionally right before every `executebackground` press, inside the same call) is what actually holds — proved by tampering `trange` to `"normal"` externally then calling `render_snapshot`, which reset it back to off and rendered one frame in ~14s, not 240. (2) `camera="fit"` was cropping geometry hard, two independent causes: an Opus subagent proved the rotation/eye math itself was already correct (reconstructed the live camera position from first principles to 6 significant figures) but found the FOV math used `aperture/aspect` alone, ignoring that a fresh camera node defaults to 1280×720 and `aspect` is pixel-not-image aspect ratio, so the assumed 45° vertical FOV was actually 26.25° — fixed by pinning the camera's `res`/`win`/`winsize` to the render resolution before computing `vertical_fov` from `resy·aperture/(resx·aspect)`; separately, the plugin's own render camera (`/obj/hmcp_cam`) turned out to carry its display flag on by default and was skewing its own framing bbox, fixed by excluding camera/light object types from `_find_displayed_geometry()`. Both fixes live-confirmed with a full-frame, correctly-proportioned render. `render_snapshot`'s response gained a `"framed"` field (nodes + bbox) for `camera="fit"`, mirroring `viewport_snapshot`'s `"viewport"` field from Stage 2, so a framing mismatch is visible in the transcript instead of silent. Not re-tested this session (already covered by Stage 0e's own finding): `rop.errors()`/`warnings()` staying empty on a modal-abort failure remains a known diagnostic gap.
- [x] 2026-08-12 — Stage 3's last two checklist items closed with Sashok directly: hand-set `prerender` on `/out/hmcp_render_karma` via Houdini's own parameter UI (since `set_parm` refuses code-bearing parms by design) → next `render_snapshot` call refused cleanly, cleared by hand afterward; dragged a parameter slider during a live background render → UI froze for ~5s, a human-hands confirmation (not just inferred from logs) that Stage 0e's "not near-instant" finding holds. Stage 3 fully closed, no open checklist items remain.
- [x] 2026-08-12 — Stage 4 of `houdini/docs/HMCP_FEEDBACK_LOOP_PLAN.md` (image delivery from pc137) closed as a documentation-only decision: checked with Sashok rather than guessing, since Stage 0-3 verification all stayed in local mode (`HMCP_HOST=127.0.0.1`) and the remote-path gap the stage exists for hasn't actually bitten yet — skipped building `scripts/fetch_render.sh`, documented the limitation in `README.md`'s new "Known limitation: image paths in remote mode" section instead, to revisit once work resumes against pc137 in remote mode.
- [x] 2026-08-12 — Stage 5 of `houdini/docs/HMCP_FEEDBACK_LOOP_PLAN.md` (`find_nodes` + delete_node undo-watermark guard, 32 → 33 commands) live-verified end to end (local 20.5.278): `find_nodes` name/type/root/max_results+truncated all confirmed against the real scene; `delete_node` watermark guard confirmed refusing after a manual Houdini edit landed between two agent calls, and confirmed re-arming after the agent's next write. Found along the way: `check_contract.py`/`check_contract_local.sh` can't connect while a Claude Code session's `houdini2` bridge is open — the plugin serves one client at a time (`server.py:183-231`) and the bridge holds a persistent singleton connection, so a second raw socket client times out waiting for `accept()`. Not a regression, just an unhandled interaction; worked around by diffing the bridge's own live `describe_commands` against `commands_spec.py` instead (33/33 agree). Stage 5 fully closed.
- [x] 2026-08-12 — `HMCP_FEEDBACK_LOOP_PLAN.md`'s own §9 housekeeping closed now that all its stages (0-5) are done: header pointers added to the three source docs (`HMCP_CAMERA_CONTROL_PLAN.md`, `HMCP_HEADLESS_RENDER_PLAN.md`, `EXTERNAL_REVIEW_oculairmedia_houdini-mcp.md`) and to `HOUDINI_MCP_REWRITE_PLAN.md`'s "After Phase 3" section, all pointing to the feedback-loop plan as the authoritative execution order; the D2 `saveViewToCamera` wording contradiction fixed in both source docs (ban is on exposing it as an agent-callable command, not on the plugin's own internal use on `/obj/hmcp_cam`). Also deployed the current hmcp package (through Stage 5, 33 commands) to pc137 via `deploy_plugin.sh hmcp` — `py_compile` clean on all 6 files — closing the deploy half of the "pc137 still pending" gap open since Stage 1; live verification there still needs Sashok's own RDP session.
- [x] 2026-08-12 — pc137 side of that gap closed: Sashok started `hmcp` on pc137 over RDP, `check_contract.py` (run directly, no HMCP_HOST override) confirmed live against `10.10.10.31:9878` — **33/33 commands agree**. `HMCP_FEEDBACK_LOOP_PLAN.md`'s Stage 1 tracker row updated accordingly. Noted for next session: this Claude Code session's `houdini2` bridge tools still address local Houdini (127.0.0.1), not pc137 — `get_scene_info` mid-check returned the local sandbox file, not a pc137 scene. Full MCP round-trip through Claude Code against pc137 would need `HMCP_HOST` repointed + a restart; deliberately skipped this session, per Sashok's call, since the raw-socket contract check already answers the open question.
- [x] 2026-08-12 — Bugfix: `little_helpers/veriter/versions.py` "Latest version" bump skipped re-checking the on-disk frame range whenever the version number was unchanged, leaving a Read stuck at a stale `first`/`last` (sh320/bg: 1001-1001 in v008 after the shot finished rendering 1001-1029 into that same v008). Now re-scans and refreshes the range even when the version folder didn't move. Deployed to pc137, no Nuke restart needed (Shift+E reloads).
- [x] 2026-08-13 — `guards.ALLOWED_NODE_CATEGORIES` opened to include `Vop` (was Sop/Object only), at Sashok's request to build VOP networks alongside VEX wrangles; deployed + `py_compile`-clean on both pc137 and local, live-verified by creating a `unifiednoise` VOP inside a fresh Attribute VOP. Live pickup needed a full Houdini restart — an in-process `sys.modules` purge + reimport twice failed to actually replace the running `guards` module (cause not root-caused).
- [x] 2026-08-13 — `promote_parm` command live-verified against `ice_scale_pattern`'s `vop_branch` (local hmcp, no bridge restart needed this time): promoted `amp_const.floatdef`, `alligator_noise.oct`, `alligator_noise.rough`, and `alligator_noise.freqx` up to the containing VOP node as `amp_const_floatdef`/`octaves`/`roughness`/`freq`; `freqy`/`freqz` linked to `freq` via `set_expression` for a single uniform-frequency slider. Render snapshot confirmed geometry unchanged.
- [x] 2026-08-13 — `ice_scale_pattern` volume branch built: `base_grid` → `polyextrude::2.0` (project down into a slab) → `vdbfrompolygons` → `volumenoisesdf` (worleyFA, displacement=normal, banding=auto) → `convertvdb` → `OUT_vol`, matching the VOP branch's curl+worley recipe on an SDF instead of a surface. Hand-built Volume VOP (bind-based read/write of the `surface` grid) never worked — perturbation silently fell outside the vdbfrompolygons narrow band and, separately, `OUT_vol`'s display/render flags got knocked off mid-session when a broken node was deleted/recreated, so render/viewport checks were misleadingly showing stale/wrong geometry for most of the debugging. Root-caused via research (SideFX docs: `volumesample()` for arbitrary-position volume reads, narrow-band voxel starvation) and fixed by switching to the purpose-built `volumenoisesdf` SOP, which handles band activation and renormalization itself. Live-verified via viewport_snapshot; reference screenshot saved as `c:/houdini_mcp_sandbox/_snapshots/curl-worley.jpg`.
- [x] 2026-08-13 — At Sashok's request for a tunable (non-black-box) alternative to `volumenoisesdf`, tried two more `ice_scale_pattern` volume branches, both left in place as-is (not fully working, per Sashok's call to stop iterating and commit current state): (1) `vol_vop_noise` (hand-built Volume VOP using a `volumesample` VOP node) — compiles clean but the `volumesample` node returns empty/no data even for a trivial identity resample (no warp), root cause not found; a `bind(Export=on)` write does work (confirmed via a large diagnostic offset), so the write side is fine, only the `volumesample` VOP read side is broken. (2) `vol_vex_noise` (`volumewrangle` VEX using `volumesample(@OpInput1, 0, pos)` + `wnoise()`, chained through `vdbrenormalizesdf` into `vol_vex_convert`/`OUT_vol_vex`) — compiles clean and does modify the SDF (confirmed: went from empty to populated geometry once the function calls were fixed — `unifiednoise()` doesn't exist as a bare VEX function, `wnoise()` needs its output-reference-argument form, not a simple return value), but the result has a regular banded/layered artifact instead of a clean single surface, likely a VDB voxel/tile-consistency issue from modifying the SDF outside `volumenoisesdf`'s own activation/renormalize handling — not resolved this session.
- [x] 2026-08-14 — Doc lifecycle established and applied: `houdini/docs/` split into durable reference (`HMCP_DESIGN.md`, `HMCP_HOUDINI_NOTES.md`) and `docs/plans/` for unbuilt work; five closed plan docs harvested and deleted (4200 → 1200 lines), all code/README/CLAUDE references rewritten, `BACKLOG.md` Done trimmed to 2026-08-09+ with the rest in `BACKLOG_ARCHIVE.md` (72KB → 39KB); rule written into root `CLAUDE.md`, including the preventive half (facts born in their permanent home, per-stage harvest, no plan-to-plan citation).
- [x] 2026-08-14 — Doc lifecycle given a trigger: three questions at the `BACKLOG` line (root `CLAUDE.md` work-log habit) as the automatic path, plus a `/harvest` project command (`.claude/commands/harvest.md`) as the manual one; `.gitignore` narrowed from `.claude/` to `.claude/*` + `!.claude/commands/` so the command is versioned while settings stay local.

## TODO
- [ ] Dedicated security review session: hmcp's write surface has grown a lot (create/delete/connect nodes, set_parm even if whitelisted, save_scene_as) since the original allowlist model was designed — worth a standalone pass on guards.py's sandbox/whitelist logic, kill-switch coverage, and what an agent could actually break on pc137 beyond a normal deploy, now that capability has outpaced the original threat model.
- [ ] `check_contract.py`/`check_contract_local.sh` can't reach the hmcp plugin while a Claude Code session's `houdini2` bridge is connected (single-client accept loop + bridge's persistent singleton connection, see 2026-08-12 Stage 5 entry above). Fine for deploy-time use with no bridge session open; consider later whether the plugin should support >1 client, or the script should route through the bridge's own connection instead of opening a fresh socket.
- [ ] Old plugin (`houdini/plugin/HoudiniMCPRender.py:24`) has the same `hou.updateMode()` bug fixed live in `hmcp/build.py` during Phase 2 verification (2026-08-10) — deprecated/uncallable on Houdini 21.0.729 (`AttributeError: No constructor defined`), needs `hou.updateModeSetting()` instead. Not fixed since the old plugin is untouched until Phase 3 retires it, but any viewport-render call on it (`viewport_flipbook`/`render_current_view`) will hit this the moment Manual-mode checking is reached.
- [ ] Function 1 (node init, per `little_helpers` repo's `docs/NUKE_COMP_LAYER_ASSEMBLY.md`'s "Knob-level деталі + офіційний init-шаблон" section) — build the confirmed template programmatically: 4 Read nodes (paths per the "Конвенція шляхів" convention) → ShuffleCopy4/9/11 (RGBA/DIR EMISSION/INDIR EMISSION, same knob recipe each time, only `in`/`out2`/`label` change) → Copy16 (tech merge) → ShuffleCopy12 (POS IN) → empty Cryptomatte + Copy24 (`mask.a` placeholder) → StickyNote labeled with the layer name. Stops there — per-object matte isolation stays manual, deliberately out of scope. (DONE 2026-08-07, see `## Done` below.)
- [ ] Function 2, remaining scope (node maintenance) — first slice (per-selected-Read latest/+/- version stepping) shipped 2026-08-07, history-node sync + info panel shipped 2026-08-07 (second slice), nodes-in-view selection fallback shipped 2026-08-07 (third slice), see `## Done` below for all three. Still open: "update whole branch to latest" as one action bumping all 4 passes together (today each selected/in-view Read is bumped independently, no branch-level grouping); a one-time sweep to apply `postage_stamp`/history-sync retroactively to Read nodes the tool hasn't touched yet (today it only applies going forward, on create/bump). Side-by-side old-beauty stacking and keeping the second-to-latest Light version nearby are explicitly deferred — Sashok's own call, discuss later.
- [ ] Bridge tool `forward_to_cc(title, body)` — CD writes structured tickets to `notes/cc_inbox.md`; CC reads on demand
- [ ] Expand `SAFE_PARMS` whitelist for LOPs (intensity/exposure/coneAngle/color/normalize on light::2.0; xform t/r/s; editproperties primpath; prune method/primpattern). See `houdini/docs/SCENE_ANALYSIS.md` for grounded parm vocabulary.
- [ ] Consider per-node-type allowlist (LightLOP vs xform vs prune — different parm sets are safe)
- [ ] Decide on `set_material` exposure in bridge (handler exists in plugin)
- [ ] Claude Desktop Project "VFX MCP" with auto-instructions to call `get_project_context`
- [ ] Decide fate of `execute_houdini_code` bridge tool (currently surfaces but plugin blocks)
- [ ] BUG 2: some /stage nodes still return `'NoneType' object has no attribute 'name'` (moon, hdri, tree_decoy, NO_PROXY_TEX1/2). Defensive `get_node_info` didn't cover the root cause — investigate with new traceback field next time CD hits it.
- [ ] Investigate `read_cc_inbox` intermittent hang reported after `execute_houdini_code` hang (2026-05-17 avp_assemble session). Hypothesis: FastMCP serialises tool calls and a previous slow tool poisons the queue. Now that `execute_houdini_code` short-circuits, this may resolve itself — re-check next session.
- [ ] More SAFE_PARMS entries as they surface (encoded `xn__` names from light::2.0 intensity/color/coneAngle/radius; rendersettings `xn__arnoldglobalGI_*`; xform `t/r/s`). Add incrementally from real scenes, not from speculation.
- [ ] Token-budget tools for big scenes:
  - `get_node_parm_names_only(path)` — list non-default parm names without values
  - `include_raw=False` flag on `get_node_info` — drop `raw_value` to save tokens
  - `parm_prefix_filter` on `get_node_info` — e.g. only `xn__arnoldglobal*`
  - `recursive=True` or new `get_node_tree(path, depth=N)` for flat sub-network walks

## Known scene-level anomalies (from `houdini/docs/SCENE_ANALYSIS.md` — informational, not infra TODOs)
- WIP rendersettings reference `/cams/cam_sh010` but no such camera (likely typo for `cam_sh110`)
- All rendersettings at 1280×1280 — preview resolution, not final
- Three bypassed production nodes: `sh110_sky`, `edit_standard_volume5`, `OUTPUT_120`
- `char_top` ≡ `char_top1` (duplicates)

## Decisions
- Hardening keeps `execute_code`, `modify_node`, `delete_node` out of dispatcher — re-enable only via narrow whitelisted tools
- Kill switches: VPN disconnect (instant) + Houdini shelf button (graceful). Nuclear SSH-kill of process holding port 9876 is a later addition
- Plugin source of truth: pc137 path. Repo holds patches and bridge. Canonical plugin checkout in repo is a future cleanup
- All editing tools must operate on whitelisted parameters/types — no broad `parm.set()` exposure

## Known limits
- `get_scene_info` caps at 10 nodes across `obj/shop/out/ch/vex/stage` (plugin `server.py` L247)
- Bridge host hardcoded `10.10.10.31`, port `9876` (bridge `houdini_mcp_server.py` L522)
- OPUS tools defined but inactive (no RapidAPI key) — they will fail at call time
