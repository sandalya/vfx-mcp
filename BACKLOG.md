# Backlog

## Done
- [x] 2026-05-16 — Bridge + plugin operational over VPN
- [x] 2026-05-16 — IP allowlist (`{127.0.0.1, 10.10.11.41}`) on plugin
- [x] 2026-05-16 — Dispatcher hardening: `execute_code`, `modify_node`, `delete_node` removed
- [x] 2026-05-16 — Auto-start removed from plugin `__init__.py`
- [x] 2026-05-17 — First live session validated (Claude Desktop ↔ pc137 sandbox via RDP)
- [x] 2026-05-17 — `set_node_parameter` (plugin + bridge) with `SAFE_PARMS` whitelist
- [x] 2026-05-17 — `get_node_info` bridge wrapper
- [x] 2026-05-17 — `parameters` passthrough in `create_node`
- [x] 2026-05-17 — `get_scene_info` cap raised to 100, added `context_filter` + `truncated` field
- [x] 2026-05-17 — Shelf-button kill switch "Stop MCP" on pc137
- [x] 2026-05-17 — OPUS code removed from bridge (was dead, no RapidAPI keys)
- [x] 2026-05-17 — Canonical local plugin copy at `vfx-mcp/plugin/server.py`
- [x] 2026-05-17 — `scripts/deploy_plugin.sh` (backup + scp + reminder)
- [x] 2026-05-17 — Bugfix: `hou.Color` serialization in `get_node_info`
- [x] 2026-05-17 — Bugfix: `parm.label()` → `parm.parmTemplate().label()`
- [x] 2026-05-17 — `get_node_info` cap removed; new `max_parms` and `only_non_default` flags
- [x] 2026-05-17 — Bugfix: defensive serialization (None-safe inputs/outputs/parms) — unblocks setvariant, collection::2.0
- [x] 2026-05-17 — First full /stage scene analyzed (`docs/SCENE_ANALYSIS.md`)
- [x] 2026-05-17 — Bridge tools `forward_to_cc` / `read_cc_inbox` (CD ↔ CC handoff via `notes/cc_inbox.md`)
- [x] 2026-05-17 — README refreshed: actual tool list + agent instructions section (CD picks up via `get_project_context`)
- [x] 2026-05-17 — Bugfix: defensive top-level `get_node_info` (ObjNode `isBypassed` AttributeError, plus `_safe()` wrapper for every field)
- [x] 2026-05-17 — Plugin error responses now include `exception_type` + full `traceback` (so CD can pinpoint future NoneTypes without round-tripping through the user)
- [x] 2026-05-17 — `scripts/dump_scene.py` (offline JSON dump of /stage and /obj — workaround for MCP token budget on big scenes)
- [x] 2026-05-17 — Bugfix: `execute_houdini_code` short-circuits in bridge (no socket round-trip) → no more 4-min MCP timeout on the disabled path
- [x] 2026-05-17 — SAFE_PARMS expansion: USD-encoded `xn__inputsexposure_vya`, `xn__arnoldglobalbucket_size_jebg`, `xn__arnoldglobalAA_samples_wcbg`
- [x] 2026-08-06 — `nuke_mcp_bridge.py` restructured to match `houdini_mcp_server.py` pattern (NukeConnection dataclass, Context, per-command timeouts, get_*_connection singleton)
- [x] 2026-08-06 — `nuke_mcp_plugin.py` deployed to pc137 `~/.nuke/`, wired via `menu.py` (manual start only, no auto-start — mirrors Houdini)
- [x] 2026-08-06 — Nuke MCP round-trip confirmed end-to-end: raw TCP ping, direct call, and real MCP protocol (list_tools/call_tool via stdio subprocess) all working against live Nuke on pc137 (port 9877). No SSH tunnel — direct VPN TCP like Houdini. Found: pc137 Windows Firewall fully OFF (all profiles) — port access is VPN reachability + app-level allowlist only, no OS rule for 9876 either.
- [x] 2026-08-06 — `nuke_mcp_plugin.py`: every MCP command now prints to Nuke's Script Editor (`[nuke-mcp HH:MM:SS] ip -> cmd(payload) -> ok/ERROR`) for live debugging; also fixed a bug where handler exceptions skipped `_audit()`. Deployed + verified live; pushed to `origin/main` (`909d28f`).
- [x] 2026-08-06 — New command `get_nodes_in_view` (plugin + bridge tool `nuke_get_nodes_in_view`): reconstructs the DAG's visible viewport rect from `nuke.center()`/`nuke.zoom()` + the `DAG_Window` Qt widget's pixel size, flags each node `in_view` by bounding-box intersection. `scripts/deploy_plugin.sh` extended to take a `<houdini|nuke|all>` target so it can deploy either plugin, each with its own backup. Deployed via hot `importlib.reload` (no Nuke restart needed) and verified round-trip on both raw TCP and the real MCP tool call.

- [x] 2026-08-06 — Hotkey-triggered overlay PoC, round 1: OS-level global hotkey (`keyboard` pkg's `WH_KEYBOARD_LL` hook, then raw `RegisterHotKey`) never fired — even synthetic `keybd_event` didn't trigger it, likely swallowed by AV/EDR (that hook shape is exactly what keyloggers use). Root-caused by isolating trigger vs. reaction: the PySide6 transparent-panel show logic was independently confirmed working (direct call + pixel-level alpha check), so the break was specifically in OS-level key detection, not the UI. Fix: register the shortcut Nuke-side instead, via `nuke.menu('Nodes').addCommand(..., 'alt+shift+p', shortcutContext=2)` calling `nuke_mcp_plugin.cmd_print(...)` directly — confirmed working end-to-end (Script Editor printed "Hotkey -> cmd_print works!"). Next: wire the actual PySide6 overlay to show via this same Nuke-native shortcut instead of a standalone process + OS hook.
- [x] 2026-08-06 — Hotkey-triggered overlay PoC, round 2 (done): moved the registration into `nuke_mcp_plugin.py` itself (`register_menu()`, idempotent via `findItem`/`removeItem`) so the dev loop is edit → deploy → hotkey, no more menu.py edits or Nuke restarts; confirmed by bumping a `TEST_ITERATION` constant and watching it update live. Landed on `ctrl+shift+a` (no `shortcutContext` override) as the working combo. Then ported the standalone `nuke_overlay.py` HUD widget (`_PrintHUD`) straight into the plugin: since Nuke 16 is itself a PySide6 app, the transparent panel now runs inside Nuke's own Qt loop on the same Nuke-native shortcut — no separate process, no socket, no OS hotkey at all. Confirmed working live by the user end-to-end.
- [x] 2026-08-06 — Nuke MCP server lifecycle: added `stop_server()` (closes the listening socket to unblock the blocking `accept()`, `_stop_requested` Event distinguishes a clean stop from a real error) and made `_server_thread`/`_server_socket`/`_stop_requested`/`_mcp_hud` reload-safe (`if 'x' not in globals()`), so `importlib.reload()` — which fires on every hotkey press — no longer orphans a live server thread or resets HUD visibility state. New dispatch commands `get_selected_nodes` (selection, Read nodes' `file` knob) and `get_env` (env vars by prefix) + matching bridge tools `nuke_get_selected_nodes`/`nuke_get_env`, for the live-scene discovery session next.
- [x] 2026-08-06 — New "MCP Server" HUD (Start/Restart/Stop stacked around the cursor; `ctrl+shift+t` or `Little Helpers/MCP Server` menu item, no hotkey required per Sashok's original ask). Bugfix along the way: `_serve()`'s "listening"/"stopped" prints never reached the Script Editor — background-thread stdout isn't captured there, same reason `_handle_client` already routes through `executeInMainThreadWithResult`; fixed the same way, plus added a status-flash label directly on the HUD so feedback doesn't depend on the Script Editor being visible. Discovered mouse clicks don't register reliably on this frameless/translucent/`Qt.Tool` window over the pc137 RDP session (Esc/keyboard events do reach it fine) — added a keyboard fallback (↑ Start, ↓ Stop, Enter/Space Restart, Esc close) as the actually-working interaction; mouse click wiring left in place but still unverified.
- [x] 2026-08-07 — `menu.py` on pc137 now calls `nuke_mcp_plugin.register_menu()` at Nuke startup (backed up first), so all hotkeys/menu items exist on a clean session with no manual `register_menu()` call — confirmed by Sashok on a fresh Nuke launch. Deliberately does NOT auto-start the socket server; that stays a manual Start in the MCP HUD, keeping the dev-convenience bridge fully decoupled from the tools themselves.
- [x] 2026-08-07 — Root-caused and fixed the MCP HUD mouse-click bug (was misdiagnosed as an RDP/translucent-window hit-testing issue): `_McpHud.show_near_cursor()` puts keyboard focus on the top-level widget itself, and `QPushButton`'s default focus policy steals focus on click — which fired the top-level's `focusOutEvent()` → `self.hide()` mid-click, before `mouseReleaseEvent` could complete the button's press/release gesture, so `clicked` never emitted. Fixed by setting `QtCore.Qt.NoFocus` on every button. Found via a from-scratch rebuild (per Sashok's steer: strip the MCP HUD down to a single Print button cloned from the already-working `_PrintHUD`, get that clicking, then build back up) plus temporary `mousePressEvent`/`eventFilter` diagnostics (removed once root-caused). Along the way also fixed a second, independent bug: `_mcp_hud`'s reload-safety (kept across `importlib.reload()` to support press-to-close) meant the widget instance was never rebuilt on redeploy, so code changes silently didn't show up until Nuke was fully restarted — now `_mcp_hud` always rebuilds fresh on every open, at the cost of the press-to-close toggle (Esc still closes). Start/Restart/Stop HUD rebuilt on the fixed base and confirmed working end-to-end by Sashok, mouse clicks included.
- [x] 2026-08-07 — Live-scene discovery on the real working scene (`sh320_v001.nk`, opened via ftrack launcher, sandbox-local path but real ftrack env), queried directly over raw TCP from Claude Code (bypassing the MCP bridge entirely — `get_env`/`get_script_info`/`get_selected_nodes` are read-only, no need to route through Claude Desktop). Findings: `$FTRACK_RENDER_PATH` and `$FTRACK_ROOT_PATH` both exist and are populated (`.../raid_echoes_of_oz/cinematic/sq010/sh320/render` and `//loky.plarium.local/project/_pl` respectively) — the earlier doc note that `$FTRACK_RENDER_PATH` "doesn't exist anywhere in the codebase" only meant not referenced in our code, not that it's unset at runtime. Project/sequence/shot come from ftrack-launcher-set env vars (`FTRACK_PROJECT_NAME`/`FTRACK_SEQUENCE_NAME`/`FTRACK_SHOT_NAME`/`FTRACK_CONTEXT`), confirmed NOT derivable from the open script's path (`C:/nuke_sandbox/sh320_v001.nk` is a local sandbox path, unrelated to the network shot path) — env vars are the correct primary source, per Sashok's call parsing the script path is a last-resort fallback only, for if the env vars are ever missing. **Found and fixed a live secret leak along the way**: `get_env` with `prefix=FTRACK` returned `FTRACK_API_KEY` (a real ftrack API key) in full over the socket. Added `SENSITIVE_ENV_NAME_PATTERNS` (`KEY`/`TOKEN`/`SECRET`/`PASSWORD`/`PWD`/`CREDENTIAL`/`AUTH`/`COOKIE`, case-insensitive substring match) to `cmd_get_env` — matching keys now return `"<redacted>"` regardless of requested prefix; deployed and confirmed live (`FTRACK_API_KEY` now redacted in a fresh query).
- [x] 2026-08-07 — New read-only plugin command `get_node_knobs` (+ bridge tool `nuke_get_node_knobs`): full knob dump via `knob.toScript()` (Nuke's own `.nk`-file serialization format) plus each node's `inputs`, for named nodes or the current selection, with an `only_non_default` filter mirroring the Houdini plugin's `get_node_info` pattern. Used it (queried directly over raw TCP, same as the live-scene discovery above) to capture exact knob-level detail for the `bg` layer-branch on `sh320` — first a real in-production example, then Sashok rebuilt the same branch into a clean, generic init template (empty Cryptomatte, placeholder channel names, a `StickyNote` branch-name marker) specifically so it could be documented as the basis for Function 1. `docs/NUKE_COMP_LAYER_ASSEMBLY.md` now has the exact step-by-step recipe (`ShuffleCopy4 "RGBA IN" → ShuffleCopy9 "DIR EMISSION" → ShuffleCopy11 "INDIR EMISSION" → Copy16 (tech merge, mix=0.48) → ShuffleCopy12 "POS IN" → Cryptomatte(empty) → Copy24 (mask.a placeholder) + StickyNote(label=layer name)`) confirmed correct by Sashok, unblocking Function 1.

- [x] 2026-08-07 — New read-only plugin command `list_render_dir` (+ bridge tool `nuke_list_render_dir`): `os.listdir()` on `$FTRACK_RENDER_PATH` (or an explicit `path` override), returning `{name, is_dir}` per entry. First step of Function 1 — direct SMB access to the render share fails from the Claude Code host (not domain-joined; VPN/TCP-445 reachable but auth denied), so this runs Nuke-side where pc137 already has working access. Deployed + hot-reloaded on live pc137 Nuke (`ctrl+shift+a`/`ctrl+shift+t`, since remote `execute_code` reload is now blocked by the auto-mode classifier as raw exec — reload has to happen from the RDP session). Confirmed live on `sh320`: 11 layer-branches under render/ (`atmo`, `bg`, `clouds`, `dem`, `dem_debris`, `dem_emis`, `dem_vol1`, `dem_vol2`, `dementor_fx`, `fg`, `floorVolume`), all folders, no sequences at that level as expected.

- [x] 2026-08-07 — Function 1 (node init) shipped: `Ctrl+Shift+A` now opens a layer-branch picker HUD (same frameless-Qt pattern as the print/MCP HUDs) listing `list_render_dir`'s entries as buttons; clicking one calls new `build_layer_branch(layer_name)`, which builds the full confirmed init template (4 Read → ShuffleCopy4/9/11 → Copy16 → ShuffleCopy12 → empty Cryptomatte → Copy24 → StickyNote, per `docs/NUKE_COMP_LAYER_ASSEMBLY.md`) centered in the current Node Graph view. `ctrl+shift+a` was reassigned from the old print-test HUD (`show_hud()`, still reachable from the menu, just no hotkey) to `show_layer_picker()`. Verified end-to-end by inspecting the live scene directly over raw TCP (bypassing RDP): node types/knobs/connections matched the reference template exactly on the first pass; two bugs found and fixed after Sashok's screenshot/manual-fix feedback — (1) node layout was wrong-axis (built left-to-right, but the real template flows top-to-bottom) — fixed by reproducing the exact relative (dx, dy) offsets pulled from the captured `sh320/bg v014` reference; (2) `os.path.join()` on the Windows (pc137) side mixed backslashes into the otherwise-forward-slash UNC render path (`render\bg/v014/...`), breaking Nuke's Read for 3 of 4 passes — fixed by building all paths via plain `f"{a}/{b}"` string join, and switched the frame-padding token from `%04d` to `####` to match Sashok's confirmed-working hand-fix. Added version/frame-range auto-resolution per pass (`_resolve_pass`/`_collapse_sequence`): highest `vNNN` folder under the layer directory that actually contains files for that pass wins (resolved independently per pass, not lock-step, per the doc's warning), files collapse into one padded sequence pattern, and any gap in the detected frame range flags the Read node (orange `tile_color` + `label` listing missing frames) instead of silently building a wrong range. StickyNote branch label is uppercased per Sashok's ask.

- [x] 2026-08-07 — Function 2, first slice (version stepping on existing Read nodes) shipped: `Ctrl+Shift+D` opens a `_VersionHUD` (same picker-HUD shell as Function 1) with 3 buttons — "Latest version", "Version +", "Version -" — acting on `nuke.selectedNodes()` filtered to `Read`, each node resolved and bumped fully independently against its own (layer, pass), per Sashok's call. New `_parse_read_file()` regex-parses `layer_dir`/`version`/`pass_name` back out of an existing Read's `file` knob (convention-based, not tied to the fixed 4-pass list, so it also parses real production/hand-edited paths); `_available_versions()` (list_render_dir's per-pass version scan, generalized from `_resolve_pass` to return the full sorted list, not just the max) finds the neighbor/latest version; `_bump_read_version()` applies it via new shared helper `_apply_read_sequence()` (factored out of `build_layer_branch`'s inline knob-setting block, now used by both). No per-button greying and no branch-level "update all 4 at once" in this slice — deliberately simple, see `## TODO` for the deferred fuller scope. Reserved an empty, unwired `QLabel` in `_VersionHUD` for a future info panel ("outdated version"/"missing frames") per Sashok's explicit "just leave room for it, don't build it yet." Along the way fixed a UX bug Sashok hit immediately on the real working scene: this HUD (unlike Function 1's, which auto-hides after one pick) stays open for repeated +/- presses, which exposed that `focusOutEvent`-based auto-close was never actually reliable for a frameless `Qt.Tool` widget over RDP — clicking the DAG didn't reliably transfer focus away. Replaced with an app-wide `QApplication` mouse-press event filter (closes on any press outside the HUD's own rect, Qt6/PySide6-`globalPosition()` vs Qt5/PySide2-`globalPos()` handled via try/except) on both `_VersionHUD` and, on Sashok's follow-up ask, retrofitted the same fix onto `_LayerPickerHUD` (Function 1), which had the identical latent issue masked only by its auto-hide-on-pick behavior. Confirmed directly in Sashok's real working scene (not just the sandbox) — reload/re-`register_menu()` in an already-running Nuke session picks up all of this with no restart needed.

- [x] 2026-08-07 — Function 2, second slice: history-node maintenance + wired-up info panel, confirmed live by Sashok. `_sync_history_reads()` keeps a row of disconnected old-version Read nodes to the left of a live beauty/lights Read in sync on every bump (`_HISTORY_COUNTS = {"lights": 1, "beauty": 5}`, tech/crypto get none) — discovers existing orphans via `_history_candidates()` (same `(layer_dir, pass_name)` + empty `node.dependent()`, deliberately not name-based, so it "adopts" hand-built history Reads like `Read78` on first run instead of duplicating them) and recomputes the desired version list from disk on every call rather than tracking state, since `_bump_read_version` mutates the live node's own knobs in place. `_VersionHUD`'s previously-reserved empty `QLabel` now shows a compact colored status (green "OK" / red short-tag list) via new `_read_status()`, refreshed on HUD open and after every button click; adds a shot-range-incomplete check (`seq["first"]/["last"]` vs `root.firstFrame()/lastFrame()`) that catches what the existing internal-gap check can't — found live on sh320/bg's beauty Read (v008, `first=last=1001` against a 1001-1029 shot, zero internal gaps but only 1 of 29 frames actually on disk). Two bugs found and fixed from Sashok's live testing: (1) `bump_selected_reads` treated every selected Read independently, including accidentally-co-selected history Reads sitting right next to the live one — a history Read getting bumped became its own "live" head and spun off its own history chain (cascading duplicates, e.g. `Read37`); fixed by skipping any selected Read with empty `node.dependent()` (not live) outright, in both the bump and the status refresh. Also had to restore the original Node Graph selection at the end of `bump_selected_reads`, since `nuke.createNode()` (invoked when history-node creation is needed) resets selection as a side effect, which would otherwise leave the artist's selection pointing at a random history Read. (2) The "outdated" check was flagging lights/tech/crypto Reads whenever they trailed behind their branch's beauty version — per the doc, the 4 passes version independently on purpose (re-render one without touching the others), so that's expected, not a problem; scoped "outdated" to the beauty pass only. Also set `postage_stamp` off by default on every Read `_apply_read_sequence` touches (live and history alike) to stop every one of them regenerating a thumbnail on every frame change, re-enabling it only on the one live beauty Read per branch (in both `build_layer_branch` and `_bump_read_version`) — existing untouched Read nodes elsewhere in the script won't retroactively change until the tool touches them.

- [x] 2026-08-07 — Function 2, third slice: `_VersionHUD` (Shift+D) now falls back to whatever's visible in the Node Graph viewport when nothing's selected, instead of doing nothing. Refactored `cmd_get_nodes_in_view`'s inline viewport math (DAG_Window pixel size + `nuke.center()`/`nuke.zoom()` reconstruction) out into `_dag_viewport_rect()` + `_nodes_in_view(node_class)`, reused by new `_working_read_nodes()` (selection if any, else `_nodes_in_view("Read")`, wrapped in try/except so a missing Node Graph panel degrades to an empty set instead of crashing the HUD) — `bump_selected_reads` and `_VersionHUD._refresh_info` both switched from reading `nuke.selectedNodes()` directly to this. Also fixed a real bug found live during the second-slice testing session: `node.dependent()` alone isn't enough to tell a live Read from a history one — a history beauty Read (`Read7`) plugged into a Viewer for visual comparison (totally normal use) still reported as having a dependent, so it got flagged/bumped as if live and spun off its own history chain. New `_is_live_read()` filters out Viewer-class dependents before deciding; used everywhere the old raw `dependent()` check was (`_history_candidates`, `bump_selected_reads`, `_refresh_info`). `cmd_get_nodes_in_view`'s own JSON response contract is unchanged (verified live over raw TCP post-refactor: 73 Read nodes total, 10 correctly flagged in_view).
- [x] 2026-08-07 — Added a "Nuke MCP bridge" section to `README.md` (topology, scaffold-phase security status, tools table, Function 1/Function 2 hotkey reference, deploy/kill-switch notes) — README previously covered Houdini only despite the Nuke bridge being a whole separate active workstream. Written in English per repo convention for reference docs.
- [x] 2026-08-07 — Function 2, fourth slice: "History" checkbox added to `_VersionHUD` (Shift+D), per Sashok's ask. Reload-safe `_HISTORY_ENABLED` global (same guard pattern as `_server_thread`) backs the checkbox so its state survives the `importlib.reload()` that fires on every hotkey press. New `_set_history_enabled(enabled)` applies immediately to `_working_read_nodes()`'s live Reads: checked syncs/creates each history-eligible pass's row via the existing `_sync_history_reads`; unchecked deletes every existing history Read found via `_history_candidates` (`nuke.delete()`) and, via the flag, stops `bump_selected_reads` from recreating them on future version steps until re-checked. Deployed to pc137 + hython-compiled clean; not yet confirmed live by Sashok on a real scene.
- [x] 2026-08-07 — Function 1 picker (`Shift+A`) overhauled per Sashok's ask: `cmd_list_render_dir` now returns each entry's `mtime`; `show_layer_picker` sorts layer-branch folders by it, freshest first, replacing the previous alphabetical order. Old one-button-per-folder layout replaced with two toggleable views sharing the same sorted name list: a `QListWidget` (click a row, builds immediately, same feel as the old buttons) and a new custom `_WheelPicker` widget -- a phone-timer-style vertical spinner (scroll or Up/Down steps the centered selection, rows fade/shrink with distance, confirm via Build/Enter/double-click) per Sashok's explicit ask for a wheel-style picker. View choice persists across HUD re-opens via reload-safe `_LAYER_PICKER_VIEW`. Freshest branch pre-selected in both views on open. Deployed to pc137 + hython-compiled clean; wheel view confirmed live by Sashok ("клас, колесо як раз те що треба") -- paint/sizing math holds up over RDP. Follow-up: arrow keys initially did nothing, since Up/Down were only ever handled in `_WheelPicker.keyPressEvent`, which never receives events -- keyboard focus stays on the top-level `_LayerPickerHUD` (everything else is deliberately `NoFocus` to dodge the focus-steal-hide bug documented elsewhere in this file), so `_WheelPicker`'s own key handling was unreachable dead code in practice. Fixed by handling Up/Down/Enter in `_LayerPickerHUD.keyPressEvent` instead and dispatching to whichever view is active via new `_step_active(delta)` (steps `self.wheel` or `self.list_widget`'s current row) -- works for both List and Wheel views now, not just Wheel. Deployed + hython-compiled clean; not yet re-confirmed live.
- [x] 2026-08-07 — Removed the List view from `_LayerPickerHUD` per Sashok's follow-up ("супер, прибери тільки list Розділ") once the wheel alone proved to cover it: dropped `self.list_widget`/`QStackedWidget`/the List-Wheel toggle row/`QButtonGroup` and the now-unused `_LAYER_PICKER_VIEW` reload-safe global, along with the QListWidget/viewToggle QSS blocks. `_WheelPicker` is now the only widget in the HUD body; `_current_selection`/`keyPressEvent` simplified to read/step it directly instead of branching on which view was active. Deployed to pc137 + hython-compiled clean; not yet re-confirmed live.

## TODO
- [ ] Function 1 (node init, per `docs/NUKE_COMP_LAYER_ASSEMBLY.md`'s "Knob-level деталі + офіційний init-шаблон" section) — build the confirmed template programmatically: 4 Read nodes (paths per the "Конвенція шляхів" convention) → ShuffleCopy4/9/11 (RGBA/DIR EMISSION/INDIR EMISSION, same knob recipe each time, only `in`/`out2`/`label` change) → Copy16 (tech merge) → ShuffleCopy12 (POS IN) → empty Cryptomatte + Copy24 (`mask.a` placeholder) → StickyNote labeled with the layer name. Stops there — per-object matte isolation stays manual, deliberately out of scope. (DONE 2026-08-07, see `## Done` below.)
- [ ] Function 2, remaining scope (node maintenance) — first slice (per-selected-Read latest/+/- version stepping) shipped 2026-08-07, history-node sync + info panel shipped 2026-08-07 (second slice), nodes-in-view selection fallback shipped 2026-08-07 (third slice), see `## Done` below for all three. Still open: "update whole branch to latest" as one action bumping all 4 passes together (today each selected/in-view Read is bumped independently, no branch-level grouping); a one-time sweep to apply `postage_stamp`/history-sync retroactively to Read nodes the tool hasn't touched yet (today it only applies going forward, on create/bump). Side-by-side old-beauty stacking and keeping the second-to-latest Light version nearby are explicitly deferred — Sashok's own call, discuss later.
- [ ] Bridge tool `forward_to_cc(title, body)` — CD writes structured tickets to `notes/cc_inbox.md`; CC reads on demand
- [ ] Expand `SAFE_PARMS` whitelist for LOPs (intensity/exposure/coneAngle/color/normalize on light::2.0; xform t/r/s; editproperties primpath; prune method/primpattern). See `docs/SCENE_ANALYSIS.md` for grounded parm vocabulary.
- [ ] Consider per-node-type allowlist (LightLOP vs xform vs prune — different parm sets are safe)
- [ ] Decide on `set_material` exposure in bridge (handler exists in plugin)
- [ ] Claude Desktop Project "VFX MCP" with auto-instructions to call `get_project_context`
- [ ] Decide fate of `execute_houdini_code` bridge tool (currently surfaces but plugin blocks)
- [ ] Update README — currently mentions OPUS/set_material/asset_lib which were removed or never exposed (DONE 2026-05-17 in `cb121cf`)
- [ ] BUG 2: some /stage nodes still return `'NoneType' object has no attribute 'name'` (moon, hdri, tree_decoy, NO_PROXY_TEX1/2). Defensive `get_node_info` didn't cover the root cause — investigate with new traceback field next time CD hits it.
- [ ] Investigate `read_cc_inbox` intermittent hang reported after `execute_houdini_code` hang (2026-05-17 avp_assemble session). Hypothesis: FastMCP serialises tool calls and a previous slow tool poisons the queue. Now that `execute_houdini_code` short-circuits, this may resolve itself — re-check next session.
- [ ] More SAFE_PARMS entries as they surface (encoded `xn__` names from light::2.0 intensity/color/coneAngle/radius; rendersettings `xn__arnoldglobalGI_*`; xform `t/r/s`). Add incrementally from real scenes, not from speculation.
- [ ] Token-budget tools for big scenes:
  - `get_node_parm_names_only(path)` — list non-default parm names without values
  - `include_raw=False` flag on `get_node_info` — drop `raw_value` to save tokens
  - `parm_prefix_filter` on `get_node_info` — e.g. only `xn__arnoldglobal*`
  - `recursive=True` or new `get_node_tree(path, depth=N)` for flat sub-network walks

## Known scene-level anomalies (from `docs/SCENE_ANALYSIS.md` — informational, not infra TODOs)
- WIP rendersettings reference `/cams/cam_sh010` but no such camera (likely typo for `cam_sh110`)
- All rendersettings at 1280×1280 — preview resolution, not final
- Three bypassed production nodes: `sh110_sky`, `edit_standard_volume5`, `OUTPUT_120`
- `char_top` ≡ `char_top1` (duplicates)

## Decisions
- Hardening keeps `execute_code`, `modify_node`, `delete_node` out of dispatcher — re-enable only via narrow whitelisted tools
- Kill switches: VPN disconnect (instant) + Houdini shelf button (graceful). Nuclear SSH-kill of process holding port 9876 is a later addition
- Plugin source of truth: pc137 path. Repo holds patches and bridge. Canonical plugin checkout in repo is a future cleanup
- All editing tools must operate on whitelisted parameters/types — no broad `parm.set()` exposure

## Known limits
- `get_scene_info` caps at 10 nodes across `obj/shop/out/ch/vex/stage` (plugin `server.py` L247)
- Bridge host hardcoded `10.10.10.31`, port `9876` (bridge `houdini_mcp_server.py` L522)
- OPUS tools defined but inactive (no RapidAPI key) — they will fail at call time
